/**
 * @type {import('next').NextConfig}
 */
const nextConfig = {
    output: 'export',
    
    // Si vous souhaitez ajouter une barre oblique finale aux chemins (facultatif)
    trailingSlash: true, 
    
    // Si vous voulez changer le répertoire de sortie (facultatif)
    // distDir: 'dist',
  }
  
  export default nextConfig;
  